# SQLite for local development; swap for a Postgres connection string in production
# (also change `provider` in prisma/schema.prisma to "postgresql")
DATABASE_URL="file:./prisma/dev.db"

# better-auth — generate a strong secret with: npx @better-auth/cli secret
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"
