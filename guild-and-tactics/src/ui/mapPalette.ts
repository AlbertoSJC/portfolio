// Shared old-map parchment palette. Used by village map and world map.

// ── Surfaces ──────────────────────────────────────────────────────────────────

/** Base parchment background. */
export const MAP_PARCHMENT = '#c8a97a';
/** Lighter parchment — node / card surfaces. */
export const MAP_PARCHMENT_LIGHT = '#e8d9bc';
/** Hovered node surface. */
export const MAP_PARCHMENT_HOVER = '#f0e6cc';
/** Active / selected node surface. */
export const MAP_PARCHMENT_ACTIVE = '#f5ecd0';
/** Cream tone — decorative lines and icon highlights. */
export const MAP_PARCHMENT_CREAM = '#e8d8a0';

// ── Ink ───────────────────────────────────────────────────────────────────────

/** Darkest ink — primary labels and fine strokes. */
export const MAP_INK = '#2c1a08';
/** Medium brown ink — paths, secondary outlines. */
export const MAP_INK_MEDIUM = '#6b4020';
/** Light ink — sublabels and captions. */
export const MAP_INK_LIGHT = '#7a4c20';

// ── Borders ───────────────────────────────────────────────────────────────────

/** Default node border. */
export const MAP_BORDER = '#8b6030';
/** Active / selected node border (strongest contrast). */
export const MAP_BORDER_ACTIVE = '#5a2e00';
/** Hovered node border. */
export const MAP_BORDER_HOVER = '#a07840';

// ── Markers ───────────────────────────────────────────────────────────────────

/** Deep red — party position marker and location pins. */
export const MAP_MARKER = '#7a2000';

// ── Icon colours ──────────────────────────────────────────────────────────────

/** Wood fill — notice boards and furniture. */
export const MAP_WOOD = '#a07040';
/** Dark wood — board strokes and outlines. */
export const MAP_WOOD_DARK = '#4a2c10';
/** Warm red-brown — pin and nail heads. */
export const MAP_WOOD_PIN = '#8b4010';

/** Darkest coin shade (bottom of stack). */
export const MAP_COIN_DARK = '#7a5a20';
/** Mid coin shade. */
export const MAP_COIN_MID = '#9a7830';
/** Brightest coin shade (top face). */
export const MAP_COIN_BRIGHT = '#b89040';
/** Coin edge stroke. */
export const MAP_COIN_STROKE = '#4a3000';

/** Side / background figure fill. */
export const MAP_FIGURE_SIDE = '#6b4820';
/** Figure outline stroke. */
export const MAP_FIGURE_STROKE = '#3a2010';
